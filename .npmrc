# Node 版本不符合 engines 时直接失败，避免"本地能跑 CI 挂掉"
engine-strict=true

# 自动安装 peerDependencies（Astro 生态依赖较重，手工补 peer 成本高）
auto-install-peers=true

# 让 peer 冲突显式暴露，而不是被静默忽略
strict-peer-dependencies=false

# 依赖提升到 workspace 根，减少重复安装
hoist-workspace-packages=true
